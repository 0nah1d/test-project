import LoginView from '@/components/views/auth/loginView'

const metadata = {
    title: 'Login',
    description: 'Login Page',
}

const LoginPage = () => {
    return (
        <div>
            <LoginView />
        </div>
    )
}

export default LoginPage
